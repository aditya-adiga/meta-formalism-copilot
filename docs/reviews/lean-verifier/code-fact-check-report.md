# Code Fact-Check Report

**Repository:** meta-formalism-copilot
**Branch:** feat/lean-verifier-graceful-degradation
**Commit:** c95c9cb50d1a3634e655700f1b6c768a6774fe9b
**Scope:** `git diff origin/main...HEAD` — 10 files (API route, hook, lib, types, components, tests)
**Checked:** 2026-04-27
**Total claims checked:** 14
**Summary:** 11 verified, 0 mostly accurate, 2 stale, 0 incorrect, 1 unverifiable

---

## Claim 1: "No verifier configured (typical on Vercel deploys without a separate verifier service)."

**Location:** `app/api/verification/lean/route.ts:28`
**Type:** Behavioral
**Verdict:** Verified
**Confidence:** High

The code reads `process.env.LEAN_VERIFIER_URL` (line 26) and, if absent/empty/undefined, returns `unavailableResponse("verifier-not-configured")` (line 29) before any fetch is attempted. Behavior matches comment.

**Evidence:** `app/api/verification/lean/route.ts:26-30`

---

## Claim 2: "Verifier reachable but errored — treat as unavailable rather than a failed proof, since the proof itself was never checked."

**Location:** `app/api/verification/lean/route.ts:46-47`
**Type:** Behavioral
**Verdict:** Verified
**Confidence:** High

When the upstream `fetch` resolves with `!res.ok`, the route returns `unavailableResponse("verifier-error", "HTTP ${res.status}")` (line 48) instead of forwarding the upstream body and status. The previous behavior (passing through the data and status) is gone, so a non-2xx upstream response no longer surfaces as a failed verification.

**Evidence:** `app/api/verification/lean/route.ts:45-49`

---

## Claim 3: "Network / timeout / DNS failure — verifier unreachable."

**Location:** `app/api/verification/lean/route.ts:54`
**Type:** Behavioral
**Verdict:** Verified
**Confidence:** High

The `try { ... } catch { ... }` wrapper is around the `fetch` call (line 36) and `AbortController` setup. A network error, DNS failure, or timeout-triggered abort would all throw and land in the `catch` block, returning `unavailableResponse("verifier-unreachable")`. The catch is broad — it would also catch a JSON-parse error on a malformed 200 response, but that is fairly described as "verifier unreachable" in spirit (the response was unusable).

**Evidence:** `app/api/verification/lean/route.ts:32-56`

---

## Claim 4: "True when the verifier is not configured or could not be reached."

**Location:** `app/lib/formalization/api.ts:107` (JSDoc on `VerifyLeanResult.unavailable`)
**Type:** Invariant
**Verdict:** Verified
**Confidence:** High

`verifyLean` (line 120) sets `unavailable: Boolean(data.unavailable)`. The route sets `unavailable: true` for all three failure modes (not-configured, unreachable, error). The doc comment slightly understates: it also covers the `verifier-error` case (configured + reachable + non-2xx), but that's a minor documentation gap, not an incorrectness — the field is still true exactly when the proof was not checked.

**Evidence:** `app/lib/formalization/api.ts:104-132`, `app/api/verification/lean/route.ts:7-14`

---

## Claim 5: "Map a verification result's `valid`/`unavailable` flags to a `VerificationStatus`. `unavailable` wins over `valid` so a missing verifier never reads as a passing proof."

**Location:** `app/lib/formalization/api.ts:111-114` (JSDoc on `verifyResultToStatus`)
**Type:** Behavioral
**Verdict:** Verified
**Confidence:** High

Implementation checks `result.unavailable` first and returns `"unavailable"` regardless of `valid`. Only when `unavailable` is falsy does it look at `valid`. The precedence ordering matches the doc.

**Evidence:** `app/lib/formalization/api.ts:115-118`

---

## Claim 6: "Only 'invalid' carries verifier output; 'valid'/'unavailable' clear errors."

**Location:** `app/hooks/useFormalizationPipeline.ts:122`
**Type:** Behavioral
**Verdict:** Verified
**Confidence:** High

Line 123 sets `vErrors = vStatus === "invalid" ? result.errors : ""`. Line 125 calls `setVerificationErrors("")` whenever `vStatus !== "invalid"`. For "valid", "unavailable", or any future non-invalid status, errors are cleared. The `verifyWithDeps` helper at line 144 mirrors this: `vErrors = vStatus === "invalid" ? (result.errors || "Verification failed") : ""`.

**Evidence:** `app/hooks/useFormalizationPipeline.ts:121-126, 142-147`

---

## Claim 7: "True when the verifier was unavailable — the proof was generated but never checked."

**Location:** `app/lib/formalization/leanRetryLoop.ts:26` (JSDoc on `LeanRetryResult.unavailable`)
**Type:** Invariant
**Verdict:** Verified
**Confidence:** High

The retry loop only sets `unavailable: true` on the path where `verifyLean` returned `unavailable=true` (line 71-77). At that point `currentCode` has already been generated by `generateLean(...)`/`generateLeanStreaming(...)` at line 59-61. So the field is true iff the proof was generated and the verification step did not check it.

**Evidence:** `app/lib/formalization/leanRetryLoop.ts:43-77`

---

## Claim 8: "No point retrying — the proof was never actually checked."

**Location:** `app/lib/formalization/leanRetryLoop.ts:74`
**Type:** Behavioral
**Verdict:** Verified
**Confidence:** High

When `unavailable` is truthy, the function `return`s immediately (line 76), exiting the `for` loop before the next iteration. No further `generateLean` calls happen. This matches the commit-message claim that the loop "short-circuits on `unavailable` so we don't burn LLM calls retrying a proof that was never checked."

**Evidence:** `app/lib/formalization/leanRetryLoop.ts:43-86`

---

## Claim 9 (commit message, 5fe0dad): "`leanRetryLoop` short-circuits on `unavailable` so we don't burn LLM calls retrying a proof that was never checked."

**Location:** Commit `5fe0dad` body
**Type:** Behavioral
**Verdict:** Verified
**Confidence:** High

Same evidence as Claim 8. The early-return path bypasses the rest of the `for (let attempt ...)` loop, so no additional `generateLean*` calls are made on the unavailable path.

**Evidence:** `app/lib/formalization/leanRetryLoop.ts:71-77`

---

## Claim 10 (commit message, 5fe0dad): "`sanitizeVerificationStatus` already strips unknown statuses to 'none', so persisted workspaces stay correct."

**Location:** Commit `5fe0dad` body
**Type:** Behavioral
**Verdict:** Verified
**Confidence:** High

`sanitizeVerificationStatus` returns `status` if it is `"valid"` or `"invalid"`, otherwise `"none"`. Since `"unavailable"` is not in the allow-list, it falls through to `"none"`. The added unit test (`workspacePersistence.test.ts:32-34`) explicitly asserts this. So a workspace persisted while the verifier was offline reloads with status `"none"`, not the now-meaningless `"unavailable"`.

**Evidence:** `app/lib/utils/workspacePersistence.ts:34-37`, `app/lib/utils/workspacePersistence.test.ts:32-34`

---

## Claim 11: "Verifier offline banner — distinct from 'Verification Failed' so users don't read a missing verifier as a passing proof."

**Location:** `app/components/features/lean-display/LeanCodeDisplay.tsx:130-131`
**Type:** Behavioral
**Verdict:** Verified
**Confidence:** High

Three rendered states are visibly distinct:
- `verificationStatus === "valid"` → green "Verified" badge (no banner) — `VerificationBadge.tsx:8-10`
- `verificationStatus === "invalid"` → red "Verification Failed" badge + red error pane — `VerificationBadge.tsx:21`, `LeanCodeDisplay.tsx:146-172`
- `verificationStatus === "unavailable"` → amber "Verifier offline — not checked" badge + amber banner — `VerificationBadge.tsx:11-20`, `LeanCodeDisplay.tsx:132-143`

The amber "unavailable" UI is rendered by a guard distinct from both the green "valid" path (no badge text would appear) and the red "invalid" path (different element, different conditional). A user encountering an offline verifier sees neither "Verified" nor a green badge.

**Evidence:** `app/components/features/lean-display/LeanCodeDisplay.tsx:132-143`, `app/components/ui/VerificationBadge.tsx:8-21`

---

## Claim 12 (test): "shows Re-verify button when status is unavailable"

**Location:** `app/components/features/lean-display/LeanCodeDisplay.test.tsx:74-82`
**Type:** Behavioral
**Verdict:** Verified
**Confidence:** High

The Re-verify button gate is `(leanEdited || verificationStatus === "invalid" || verificationStatus === "unavailable") && editMode === "rendered"`, also requires `code` to be truthy. The test passes `defaultProps` (`code: 'theorem hello : True := trivial'`) and only overrides `verificationStatus="unavailable"`. `editMode` initial state is `"rendered"`, `leanEdited` starts `false`. The middle disjunct fires, so the button renders.

**Evidence:** `app/components/features/lean-display/LeanCodeDisplay.tsx:109-119`, `LeanCodeDisplay.test.tsx:7-14, 74-82`

---

## Claim 13 (README.md): "The Next.js route reads `LEAN_VERIFIER_URL` from the environment (defaults to `http://localhost:3100`)."

**Location:** `README.md:84`
**Type:** Configuration
**Verdict:** Stale
**Confidence:** High

The branch removes the `?? "http://localhost:3100"` fallback. The route now treats an unset `LEAN_VERIFIER_URL` as "not configured" and returns the unavailable response without contacting any host. There is no longer a default URL. This README sentence was correct before the branch and is now wrong.

**Evidence:** `app/api/verification/lean/route.ts:26-30` (no default), prior code had `const LEAN_VERIFIER_URL = process.env.LEAN_VERIFIER_URL ?? "http://localhost:3100";`

---

## Claim 14 (README.md): "The app continues to work without the verifier — the API route falls back to a mock `{ valid: true, mock: true }` response."

**Location:** `README.md:92`
**Type:** Behavioral
**Verdict:** Stale
**Confidence:** High

This is the exact behavior the branch removes. The route no longer returns `{ valid: true, mock: true }` on any path; instead it returns `{ valid: false, unavailable: true, reason: "verifier-..." }`. A reader following this README would still expect the green-pass mock fallback that no longer exists.

The very similar fallback claim in `docs/ARCHITECTURE.md:200-205` ("`If verifier is unavailable: ← fallback mock { valid: true, mock: true }`") is also stale for the same reason — flagging it here as a related staleness.

**Evidence:** `app/api/verification/lean/route.ts:7-14, 26-56`, `README.md:92`, `docs/ARCHITECTURE.md:200-205`

---

## Claim 15 (LeanCodeDisplay banner copy): "Set the `LEAN_VERIFIER_URL` environment variable to a running verifier to enable checking."

**Location:** `app/components/features/lean-display/LeanCodeDisplay.tsx:138-140`
**Type:** Configuration
**Verdict:** Unverifiable
**Confidence:** Medium

The instruction is correct as a literal statement about the env var name (`process.env.LEAN_VERIFIER_URL` is what the route reads — verified). What I can't verify from the code alone is whether "a running verifier" suffices in deployed environments — i.e., whether Vercel deploys can actually reach an arbitrary `LEAN_VERIFIER_URL` (network egress, container networking) such that following this instruction would "enable checking" in the user's environment. That is a deployment/infra fact, not a code fact. The narrower behavioral claim — "setting `LEAN_VERIFIER_URL` causes the route to attempt a fetch to that URL" — is verified.

**Evidence:** `app/api/verification/lean/route.ts:26-41`

---

## Claims Requiring Attention

### Stale
- **Claim 13** (`README.md:84`): Documents a `http://localhost:3100` default that no longer exists in the route.
- **Claim 14** (`README.md:92`, also `docs/ARCHITECTURE.md:200-205`): Documents the now-removed `{ valid: true, mock: true }` fallback. New behavior surfaces an "unavailable" UI state instead.

### Unverifiable
- **Claim 15** (`LeanCodeDisplay.tsx:138-140`): Instruction is correct about the env var name; whether following it suffices in any given deployment is a deploy/infra question outside static analysis.

### Notes
- The two stale README/ARCHITECTURE entries are the same documentation update; addressing them in this branch (or a follow-up doc PR) would close the gap. They are stale by virtue of this branch's behavior change, not pre-existing rot.
- No incorrect claims found.
- The PR/commit messages and inline JSDoc/comments accurately describe the implemented behavior.
